# Eval Fixtures

Hold-out prompts, expected patterns, and rubric inputs belong here.

Keep evaluation cases separate from training inputs. Split by challenge family rather than by individual rows whenever possible.

The checked-in `example_cases.jsonl` file is a tiny synthetic fixture for smoke-testing `scripts/run_eval.py`. Real eval sets should be larger and remain challenge-split.
